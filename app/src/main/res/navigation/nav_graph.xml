<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/weatherFragment">

    <fragment
        android:id="@+id/weatherFragment"
        android:label="Выбрать город"
        android:name="ru.lonelywh1te.justweather.WeatherFragment"
        tools:layout="@layout/fragment_weather" >
        <action
            android:id="@+id/to_searchCityFragment"
            app:destination="@id/searchCityFragment" />
    </fragment>
    <fragment
        android:id="@+id/searchCityFragment"
        android:name="ru.lonelywh1te.justweather.SearchCityFragment"
        tools:layout="@layout/fragment_search_city" />

</navigation>